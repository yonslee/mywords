// import { useParams } from "react-router-dom"

const Word = () => {
// 사용자 정의(=커스텀) 훅스.
// 전달받아서 들어오는 path variable을 모아서 객체로 갖다준다.
// app.js에서 id라고 정의했으므로 const에서도 id로 꺼내온다.
    // const {id} = useParams();

    return

}

export default Word